---
import BaseLayout from './BaseLayout.astro';
import '@styles/tuner.css';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  keywords?: string;
  schema?: Record<string, unknown>[];
}

const { title, description, canonical, keywords, schema = [] } = Astro.props;
---

<BaseLayout {title} {description} {canonical} {keywords} {schema}>
  <div class="min-h-screen bg-gradient-to-br from-violet-600 via-purple-600 to-fuchsia-600 text-white">
    <slot />
  </div>
</BaseLayout>
